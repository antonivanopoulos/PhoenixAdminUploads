defmodule UploadTest.RegistrationsControllerTest do
  use UploadTest.ConnCase

  # test "POST create", %{conn: conn} do
  #   conn = post conn, "/api/categories"
  #   assert json_response(conn, 200)
  # end
end
